import styled from 'styled-components';

export const Log = styled.div`
  column-count: 3;
  
  & span {
    font-size: 0.7em;
    display: block;
  }
  & ol {
    font-size: 0.7em;
    margin: 0;
    padding-left: 20px;
  }
  & h3 {
    font-size: 0.9em;
  }
`;